import "server-only";

/**
 * Notify buyers whose wishlists match a newly-active listing (email + in-platform
 * notification). Stub for now — fully implemented in Step 12. Called from the
 * admin approve action.
 */
export async function notifyWishlistMatches(_listingId: string): Promise<void> {
  // no-op until Step 12
}
